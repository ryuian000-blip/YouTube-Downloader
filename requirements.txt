PySide6
yt-dlp
pillow
pyinstaller

# Agent-facing surfaces (ytdl_cli.py / ytdl_mcp.py). The GUI does not
# import these -- a plain `python main.py` still runs without them.
mcp                 # MCP server SDK (2.x: FastMCP is now MCPServer)
faster-whisper      # local transcription, only for videos with no captions
pytest              # the test suite in tests/

# yt-dlp's JS challenge solver scripts. Deno alone (the bundled binary) is
# just the engine -- without this package there are no solver scripts for
# it to run, YouTube's nsig/PO-token challenges silently fail, and most
# clients either list no usable formats ("Only images are available") or
# hand back URLs that 403. This was exactly the missing piece behind
# mid-download 403s in August 2026.
yt-dlp-ejs

# NOTE: the plain "yt-dlp" line above resolves to the latest STABLE
# release, which can lag YouTube's anti-bot/streaming changes by weeks --
# long enough to break all downloads (YouTube's SABR-only rollout broke
# stable 2026.7.4 while that was still the newest stable). The build
# scripts (build.bat / build.command / build.yml) therefore upgrade to
# yt-dlp's NIGHTLY channel after installing this file:
#   pip install -U --pre "yt-dlp[default]"
# Do the same in a dev venv if downloads start 403ing.
