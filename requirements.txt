PySide6
yt-dlp
pillow
pyinstaller

# yt-dlp's JS-challenge-solver dependency has been renamed/restructured
# before -- confirm the current package name in yt-dlp's own docs
# (https://github.com/yt-dlp/yt-dlp/wiki) before shipping, and add it here.
# Deno itself is a standalone binary (bundled via PyInstaller --add-binary
# / build.spec), not a pip package -- this line is only for whatever small
# Python-side shim yt-dlp currently needs, if any.
